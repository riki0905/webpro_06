<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Even or Odd</title>
</head>
<body>
  <h1>偶数・奇数判定</h1>
  <form action="/evenodd" method="get">
    <label for="number">数字を入力してください:</label>
    <input type="number" id="number" name="number" required>
    <button type="submit">判定</button>
  </form>
  <% if (number !== null) { %>
    <p><strong>入力された数字:</strong> <%= number %></p>
    <p><strong>結果:</strong> <%= result %></p>
  <% } else if (result) { %>
    <p><strong>結果:</strong> <%= result %></p>
  <% } %>
</body>
</html>
